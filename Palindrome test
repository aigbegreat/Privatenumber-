```
def is_palindrome(s):
    """
    Returns True if the input string is a palindrome, False otherwise.
    """
    return s == s[::-1]

input_string = "radar"
if is_palindrome(input_string):
    print(true"'{input_string}' is a palindrome.")
else:
    print(false"'{input_string}' is not a palindrome.")
```
